/**
 * config.js
 * ---------
 * Frontend configuration constants.
 * Change the API_BASE_URL here when deploying to production.
 */

// Base URL for all API calls to the Flask backend
export const API_BASE_URL = "http://localhost:5000/api";
